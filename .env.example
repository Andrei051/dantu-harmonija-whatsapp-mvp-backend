PORT=3000
WHATSAPP_VERIFY_TOKEN=your-verify-token
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=

# Phase 2C / 3A AI (optional — copy to .env)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Phase 3A — sole v2 gate (false = frozen v1.1)
AI_ENABLED=false
# Temporary resilience check for anchor #11: api | schema (leave empty otherwise)
AI_FORCE_FAIL=

# Optional disk path for bounded conversation turns (defaults to OS tmp)
# WHATSAPP_CONVERSATION_STATE_PATH=
